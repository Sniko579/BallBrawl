using UnityEngine;

public class RollingTest : MonoBehaviour
{
    
    void Start()
    {
        //transform.position = new Vector2(Movement.Radius, transform.position.y);    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
